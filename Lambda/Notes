What is Lambda ?

It is a platform as service service in AWS it executes the code where we can deploy the AWS Resources in supports languages Python, Node.js, Ruby this service will charge only the execution time of the code
