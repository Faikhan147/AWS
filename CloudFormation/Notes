What is CloudFormation ?

It is a Infrastructure as Code service in AWS where we can deploy the AWS resources. Simply we have to create the template and based on that it creates the infrastructure in AWS accordingly.
